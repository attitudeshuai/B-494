<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.lostandfound.mapper.FoundItemMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO found_items (name, place, found_time, description, image_path, user_id, status, create_time)
        VALUES (#{name}, #{place}, #{foundTime}, #{description}, #{imagePath}, #{userId}, #{status}, #{createTime})
    </insert>

    <update id="updateStatus">
        UPDATE found_items SET status = #{status} WHERE id = #{id}
    </update>

    <update id="update">
        UPDATE found_items
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="place != null">place = #{place},</if>
            <if test="foundTime != null">found_time = #{foundTime},</if>
            <if test="description != null">description = #{description},</if>
            <if test="imagePath != null">image_path = #{imagePath},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectList" resultType="com.school.lostandfound.entity.FoundItem">
        SELECT i.*, u.name as user_name 
        FROM found_items i 
        LEFT JOIN users u ON i.user_id = u.id
        <where>
            <if test="status != null">
                AND i.status = #{status}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (i.name LIKE CONCAT('%', #{keyword}, '%') 
                     OR i.place LIKE CONCAT('%', #{keyword}, '%')
                     OR i.description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY i.create_time DESC
    </select>

    <select id="selectById" resultType="com.school.lostandfound.entity.FoundItem">
        SELECT i.*, u.name as user_name 
        FROM found_items i 
        LEFT JOIN users u ON i.user_id = u.id
        WHERE i.id = #{id}
    </select>

    <select id="selectByUserId" resultType="com.school.lostandfound.entity.FoundItem">
        SELECT i.*, u.name as user_name 
        FROM found_items i 
        LEFT JOIN users u ON i.user_id = u.id
        WHERE i.user_id = #{userId}
        ORDER BY i.create_time DESC
    </select>
    
    <delete id="deleteById">
        DELETE FROM found_items WHERE id = #{id}
    </delete>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM found_items
    </select>

    <select id="selectSimilar" resultType="com.school.lostandfound.entity.FoundItem">
        SELECT * FROM found_items
        WHERE status = 1
        AND (
            name LIKE CONCAT('%', #{name}, '%')
            OR place LIKE CONCAT('%', #{place}, '%')
        )
        ORDER BY create_time DESC
        LIMIT 5
    </select>

    <select id="selectDailyStats" resultType="java.util.Map">
        SELECT DATE(FROM_UNIXTIME(create_time/1000)) as date, COUNT(*) as count
        FROM found_items
        GROUP BY DATE(FROM_UNIXTIME(create_time/1000))
        ORDER BY date DESC
        LIMIT 7
    </select>
</mapper>
